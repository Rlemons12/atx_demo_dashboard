apiVersion: 1

apps:
  - type: grafana-llm-app
    org_id: 1
    disabled: false
    jsonData:
      provider: openai
      openAI:
        provider: openai
        disabled: false
    secureJsonData:
      openAIKey: ${OPENAI_API_KEY}
